<li class="<%= 'active' if page.current? %> waves-effect">
  <%= link_to page, page.current? ? '#' : url,
              { remote: remote, rel: page.next? ? 'next' : page.prev? ? 'prev' : nil } %>
</li>
