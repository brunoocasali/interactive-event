<%= simple_form_for(@user, url: admin_users_path) do |f| %>
  <%= f.error_notification %>

  <div class="row">
    <div class="row">
      <div class="input-field col s12">
        <%= f.input :name, input_html: { class: 'validate' } %>
      </div>
    </div>
    <div class="row">
      <div class="input-field col s12">
        <%= f.input :email, as: :email, input_html: { class: 'validate' } %>
      </div>
    </div>
    <div class="row">
      <div class="input-field col s12">
        <%= f.input :phone %>
      </div>
    </div>
    <div class="row">
      <div class=" col s12">
        <%= f.association :role, collection: Role.all %>
      </div>
    </div>
    <div class="row">
      <div class="col s12">
        <%= f.input :password, input_html: { class: 'validate' } %>
      </div>
    </div>
  </div>

  <%= fab_to_save back_url: admin_users_url %>
<% end %>
